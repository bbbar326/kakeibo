<%= form_for(receipt_detail) do |f| %>
  <% if receipt_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(receipt_detail.errors.count, "error") %> prohibited this receipt_detail from being saved:</h2>

      <ul>
      <% receipt_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :expense_id %>
    <%= f.number_field :expense_id %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.number_field :price %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

<!-- receipt -->
  <div class="field">
    <%= f.label :receipt_id %>
    <%= f.number_field :receipt_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
