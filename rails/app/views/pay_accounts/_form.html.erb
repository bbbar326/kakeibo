<%= form_for(pay_account) do |f| %>
  <% if pay_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pay_account.errors.count, "error") %> prohibited this pay_account from being saved:</h2>

      <ul>
      <% pay_account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
